[project]
name = "shibaclaw-plugin-dungeon-chaos"
version = "0.0.0"
description = "Experimental plugin (fork)"
requires-python = ">=3.12"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["dungeon_chaos"]
